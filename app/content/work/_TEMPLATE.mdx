---
title: "{{TODO: project title, e.g. \"Office Network Rebuild for a 12-Person Studio\"}}"
summary: "{{TODO: one sentence — what this project was and the outcome}}"
date: "{{TODO: YYYY-MM-DD}}"
tags: ["{{TODO: e.g. UniFi}}", "{{TODO: e.g. Small Business}}"]
# Optional — delete this whole block if you don't have a client quote yet.
# Never invent one; only use a real quote with the client's permission.
clientQuote:
  quote: "{{TODO: the actual quote}}"
  author: "{{TODO: name, or \"Client\" if anonymous}}"
# Optional — paths under public/, e.g. "work/studio-rebuild/rack.jpg".
photos: []
---

<!--
  This file is a template, not a published case study — files starting
  with "_" are excluded from the site (see lib/work.ts). Copy this file to
  a new one without the leading underscore (e.g. studio-rebuild.mdx) and
  fill in the sections below. Delete any section that doesn't apply, but
  keep the overall problem -> design -> build -> result shape — that's
  what makes a case study different from a Learn article.
-->

## The problem

{{TODO: What wasn't working, in the client's own words if you can. What was actually happening — dead zones, an unreliable network, guest devices with too much access, whatever the real starting point was.}}

## The design

{{TODO: What you proposed and why — the tradeoffs you considered, what you ruled out, and what the plan actually was before any hardware went in.}}

## The build

{{TODO: What actually happened during the install/deployment. Real constraints you hit, anything that didn't go according to plan, and how it was handled.}}

## The result

{{TODO: What changed. Be specific and honest — real numbers or outcomes if you have them, not vague claims.}}
