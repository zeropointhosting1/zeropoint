// Real client testimonials only — never invented. Empty until you add one;
// the Testimonials component renders nothing while this is empty.
export type Testimonial = {
  quote: string
  author: string
  context: string
}

export const TESTIMONIALS: Testimonial[] = []
